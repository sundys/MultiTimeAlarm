# 更新日志

每个版本的更新及修改摘要。自动发布流程会取最顶部小节作为 Release 说明。

## v1.1.6

### 变更
- 闹钟列表卡片第二行统一为「x月x日 几点几分 + 提醒类型 + N个时间点」三项：
  - 下次响铃时间改为「9月4日 13:41」中文格式
  - 「仅一次」类型只显示类型文字，不再把日期拼进类型（修复「仅一次9月4日」导致行过长的问题）

## v1.1.5

### 修复
- 修复每月提醒跨月取错日期的问题：当月已无剩余提醒日期时，正确顺延到下月的首个提醒日期（此前可能取到错误候选导致不响或早响）
- 闹钟列表卡片改为两行紧凑样式：第一行闹钟名称，第二行「下次响铃时间 / 提醒类型（每天/工作日/每月等）/ N个时间点」，不再逐行显示全部日期；名称与时间超长时自动省略号截断，不再撑长列表导致错位

### 变更
- 每月提醒支持多日期与范围表达式（如 1,3,9,25 / 1-15 / 组合式），输入全角符号自动转换，保存时校验已过日期与当月不存在的日期

## v1.1.4

### 新增
- 「关于」页新增开源地址入口，点击跳转本仓库 GitHub 页面
- 检测到新版本时可直接下载更新：自动选择匹配设备 ABI 的正式签名安装包，经多镜像加速下载到系统 Download 目录，带下载进度显示，下载完成后在文件管理器点击即可安装

### 变更
- 「关于」页信息对齐展示：当前版本 / 检测更新 / 开源地址 三行左对齐
- 检测更新支持多镜像地址自动回退（gh-proxy.com / gh.halonice.com / gh.slw.im / 直连），并带显式超时与失败原因提示

### 修复
- 修复应用缺少 INTERNET 权限导致检测更新必然失败的问题

## v1.1.3

### 新增
- 数据管理：设置页新增「备份闹钟」「恢复闹钟」「清除全部闹钟」
  - 备份导出 JSON 文件（系统文件保存器，文件名自带时间戳），恢复从备份文件导入并自动重新注册全部系统闹钟（覆盖式，带确认）
  - 清除全部闹钟带二次确认，删除所有任务、提醒时间并取消系统闹钟注册
- 闹钟备注：编辑闹钟可填写备注，列表卡片显示于名称下方
- 编辑闹钟时点击时间数字即可重新选择该时间点（此前只能删除重加）
- 时间选择器打开时默认选中当前时间；时/分/秒支持循环滚动（23→00、59→00 无缝回绕）
- 「仅一次」日期选择改为年/月/日三列循环滚动小窗（紧凑不超宽），年份范围 2025–2099，默认选中当天

### 变更
- 闹钟列表卡片改为两行紧凑样式：第一行闹钟名称+总开关，第二行「下次响铃时间 / 提醒类型 / N个时间点」，不再逐行显示全部时间点
- 闹钟列表按下一次响铃时间升序排序（多时间点取最早的一个，无未来触发的沉底）
- 移除卡片删除按钮，改为长按卡片弹出删除确认
- 时间/日期选择器仅显示中间三行数字，界面更简洁
- 响铃页「贪睡 X 分钟」改为「再等 X 分钟」
- 设置页/编辑页按系统返回键返回首页，不再退出应用
- 设置页菜单分类重组：通用（主题颜色、铃声设置）/ 权限（全屏弹窗、后台弹窗、忽略电池优化、自启动）/ 数据（备份、恢复、清除）/ 关于，并支持整页滚动
- 新增「关于」对话框：应用介绍、当前版本显示与检测更新（经代理多地址自动回退请求仓库最新 Release）

### 修复
- 修复编辑页按钮过长、贪睡输入框过高导致的排版松散问题
- 修复选择器初始化定位偏移一格、状态被重置为初始项的问题
- 修复设置页超出屏幕后无法滚动查看底部菜单的问题
- 修复检测更新失败的问题（补充 INTERNET 权限声明，多镜像地址自动回退与显式超时）

## v1.1.2（正式版）

> **重要**：本版本起应用包名变更为 `com.sundys.alarm`，与旧版（v1.1.0 / v1.1.1，包名 `com.example.multitimealarm`）不互相覆盖升级，请先卸载旧版再安装。旧两个版本已合并至本版发布并删除。

### 应用信息
- 应用包名变更：`com.example.multitimealarm` → `com.sundys.alarm`

### 闹钟核心功能（累计）
- 一个闹钟可设置多个提醒时间点，统一管理、单独启停
- 五种提醒类型：每天重复 / 按星期（工作日、周末、每天预设）/ 每月某日 / 间隔循环 / 仅一次
- 提醒时间精确到秒：时、分、秒三列数字滚动选择器
- 贪睡可配置：间隔（分钟）与次数上限（0 = 不限）
- 小憩倒计时：一键 N 分钟后响铃，独立 Tab 管理
- 闹钟铃声设置：系统铃声选择器，支持默认 / 静音（仅振动）/ 本地铃声

### 提醒可靠性（国产 ROM 深度兼容）
- 精确闹钟权限自动授予（USE_EXACT_ALARM），修复 ColorOS 15 上调度抛 SecurityException 导致的闪退
- 响铃改由前台服务承载：进程为前台重要性，铃声不因系统缓存回收中断，即使响铃页被 ROM 拦截也持续响铃
- 高优先级通知常驻，点击通知直达响铃页
- 设置页一键授权引导：全屏弹窗权限、后台弹窗权限（悬浮窗）、忽略电池优化、自启动设置
- 开机广播与冷启动双重兜底重调度；调度链路异常只记日志不再引发闪退循环

### 界面
- 闹钟 / 小憩双 Tab 首页，滑动切换，胶囊形指示器
- 浮动圆形 + 按钮：列表滑动时自动隐藏、停下显示
- 主题颜色三模式：跟随系统 / 浅色 / 暗色，状态栏与导航栏配色同步跟随
- 系统返回键在设置页 / 编辑页返回首页而非退出应用

### 问题修复
- 修复"新建闹钟"点击无反应的导航状态冲突
- 修复一次性任务贪睡后不再响铃的问题
- 修复贪睡闹钟覆盖已续订主闹钟的问题
- 修复暗色主题下状态栏 / 导航栏颜色不一致的问题
- 修复小憩对话框预设选项被裁剪的问题

### 发布形态
- Release 仅提供正式签名 APK：arm64-v8a 与 armeabi-v7a 两个 ABI
